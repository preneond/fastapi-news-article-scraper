version: '3.4'
services:
  postgres:
    image: postgres:10
    ports:
      - 5432:5432
    restart: on-failure
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
